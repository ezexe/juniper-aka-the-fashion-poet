<UserControl x:Class="Juniper.WinUI.App.Controls.PageHeaderUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Juniper.WinUI.App.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="headerControl"
             VerticalAlignment="Top"
             Foreground="{ThemeResource PageHeaderForegroundBrush}"
             Background="Transparent"
             FontSize="28"
             IsHitTestVisible="False">

    <Grid>
        <!--<Rectangle x:Name="NarrowBackground"
                   Visibility="Collapsed" />

        <Rectangle x:Name="WideBackground"
                   Fill="{Binding ElementName=headerControl, Path=Background}"
                   Opacity="{Binding ElementName=headerControl, Path=BackgroundColorOpacity}" />-->

        <Grid x:Name="headerRoot"
              Padding="{Binding ElementName=headerControl, Path=Padding}"
              VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid x:Name="pageTitle"
                  Background="Transparent"
                  VerticalAlignment="Top">
                <Canvas x:Name="ShadowHost"
                        Opacity="{x:Bind ShadowOpacity, Mode=OneWay}" />
                <TextBlock x:Name="TitleTextBlock"
                           Style="{StaticResource TitleTextBlockStyle11}"
                           VerticalAlignment="Center"
                           FontSize="{Binding ElementName=headerControl, Path=FontSize}"
                           Foreground="{Binding ElementName=headerControl, Path=Foreground}"
                           Text="{x:Bind Title, Mode=OneWay}"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="NoWrap" />
            </Grid>
            <ContentPresenter  Grid.Row="0"
                               Grid.Column="1"
                               Content="{x:Bind AppBarButtonsContent,Mode=OneWay}" />
            <!--<ContentPresenter Grid.Row="1"
                              Margin="0,6,0,12"
                              Grid.ColumnSpan="2"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Stretch"
                              HorizontalContentAlignment="Stretch"
                              Content="{x:Bind AppBarButtonsContent}" />-->
            <!--<Border x:Name="commandBarBorder"
                    Grid.Column="1"
                    VerticalAlignment="Top">
                <ContentPresenter x:Name="ControlPresenter"
                                  Content="{x:Bind AppBarButtonsContent}" />
            </Border>-->
        </Grid>
    </Grid>
</UserControl>
