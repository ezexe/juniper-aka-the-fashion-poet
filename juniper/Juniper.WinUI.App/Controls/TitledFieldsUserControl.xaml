<UserControl x:Class="Juniper.WinUI.App.Controls.TitledFieldsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Juniper.WinUI.App.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <x:Double x:Key="ExampleMaxWidth">1000</x:Double>

        <Storyboard x:Name="PopOutStoryboard">
            <PopInThemeAnimation SpeedRatio="2"
                                 TargetName="rootGrid" />
        </Storyboard>

        <Storyboard x:Name="PopInStoryboard">
            <PopInThemeAnimation FromVerticalOffset="152"
                                 SpeedRatio=".2"
                                 TargetName="rootGrid" />
        </Storyboard>
    </UserControl.Resources>

    <Grid x:Name="rootGrid"
          Loaded="rootGrid_Loaded">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="IntermediateLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="740" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="PhoneLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="OptionsPresenter.HorizontalContentAlignment"
                                Value="Left" />
                        <Setter Target="OptionsPresenter.Margin"
                                Value="0,24,0,0" />
                        <Setter Target="OptionsPresenter.(Grid.Row)"
                                Value="1" />
                        <Setter Target="OptionsPresenter.(Grid.Column)"
                                Value="0" />
                        <Setter Target="OptionsPresenter.(Grid.ColumnSpan)"
                                Value="2" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>

            <VisualStateGroup>
                <VisualState x:Name="NormalMode" />
                <VisualState x:Name="ScreenshotMode">
                    <VisualState.Setters>
                        <Setter Target="CodeScrollViewer.Visibility"
                                Value="Collapsed" />
                        <Setter Target="ControlPresenter.Padding"
                                Value="{ThemeResource ControlElementScreenshotModePadding}" />
                        <Setter Target="ControlPresenter.HorizontalAlignment"
                                Value="Center" />
                        <Setter Target="ScreenshotControls.Visibility"
                                Value="Visible" />

                        <Setter Target="ExampleContainer.Background"
                                Value="{ThemeResource SystemControlPageBackgroundBaseLowBrush}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>

            <VisualStateGroup x:Name="SampleCodeSeparatorStates">
                <VisualState x:Name="SeparatorCollapsed" />
                <VisualState x:Name="SeparatorVisible">
                    <VisualState.Setters>
                        <Setter Target="SampleCodeSeparator.Visibility"
                                Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}"
                   Margin="4,8"
                   Text="{x:Bind HeaderText}" />

        <TextBlock x:Name="tbErrorText"
                   Grid.Row="1"
                   Padding="0,0,0,0"
                   Margin="0,0,0,12"
                   Visibility="Collapsed"
                   FontWeight="Normal"
                   IsTextSelectionEnabled="True"
                   Foreground="{ThemeResource SystemFillColorCriticalBrush}" />


        <Grid Grid.Row="2"
              CornerRadius="{StaticResource ControlCornerRadius}">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid x:Name="ExampleContainer"
                  x:FieldModifier="Public"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                  BorderThickness="{StaticResource ControlExampleDisplayBorderThickness}"
                  CornerRadius="{StaticResource ControlCornerRadius}"
                  Background="{ThemeResource ControlExampleDisplayBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto"
                                      MaxWidth="320" />
                    <ColumnDefinition Width="Auto"
                                      MaxWidth="320" />
                </Grid.ColumnDefinitions>

                <!--  Display  -->
                <ContentPresenter x:Name="ControlPresenter"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  Padding="6"
                                  HorizontalContentAlignment="{x:Bind HorizontalContentAlignment}"
                                  Content="{x:Bind Fields}" />

                <!--  Output Display  -->
                <StackPanel Orientation="Vertical"
                            Grid.Column="1"
                            Margin="0,12,12,12"
                            Padding="16"
                            CornerRadius="{StaticResource ControlCornerRadius}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Stretch"
                            Background="{ThemeResource ControlExampleDisplayBrush}"
                            Visibility="{x:Bind Output, Converter={StaticResource nullToVisibilityConverter}}">
                    <TextBlock Text="Output:" />
                    <ContentPresenter x:Name="OutputPresenter"
                                      Content="{x:Bind Output}" />
                </StackPanel>

                <!--  Options Display  -->
                <ContentPresenter x:Name="OptionsPresenter"
                                  Grid.Column="2"
                                  CornerRadius="{StaticResource ControlCornerRadius}"
                                  Margin="0,12,12,12"
                                  Padding="6"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                  Content="{x:Bind Options}"
                                  Visibility="{x:Bind Options, Converter={StaticResource nullToVisibilityConverter}}" />
            </Grid>

            <!--  Sample code display  -->
            <Expander Grid.Row="1"
                      Visibility="Collapsed"
                           Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                           CornerRadius="0,0,8,8"
                           HorizontalAlignment="Stretch"
                           HorizontalContentAlignment="Stretch">

                <Expander.Header>
                    <TextBlock Text="Source code" />
                </Expander.Header>

                <StackPanel
                    x:DefaultBindMode="OneWay">
                    <Border
                        x:Name="SampleCodeSeparator"
                        Visibility="Collapsed"
                        Margin="0,20"
                        BorderThickness="{StaticResource ControlBorderThickness}"
                        BorderBrush="{ThemeResource SystemControlBackgroundBaseMediumBrush}" />
                </StackPanel>
            </Expander>

            <StackPanel x:Name="ScreenshotControls"
                        Grid.Row="2"
                        Orientation="Horizontal"
                        Spacing="6"
                        Margin="0,6,0,0"
                        Visibility="Collapsed">
                <Button x:Name="ScreenshotButton"
                        Content="Take Screenshot"
                        HorizontalAlignment="Left"
                        Click="ScreenshotButton_Click" />

                <Button x:Name="ScreenshotDelayButton"
                        Content="Take Screenshot With Delay"
                        HorizontalAlignment="Left"
                        Click="ScreenshotDelayButton_Click" />

                <TextBlock x:Name="ScreenshotStatusTextBlock"
                           VerticalAlignment="Center"
                           Margin="0,0,0,1" />

                <TextBlock x:Name="PaddingLabel"
                           Text="Padding:"
                           VerticalAlignment="Center"
                           Margin="5,0,5,1" />
                <TextBox x:Name="ControlPaddingBox"
                         AutomationProperties.LabeledBy="{x:Bind PaddingLabel}"
                         Width="150"
                         KeyUp="ControlPaddingBox_KeyUp"
                         LostFocus="ControlPaddingBox_LostFocus" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
