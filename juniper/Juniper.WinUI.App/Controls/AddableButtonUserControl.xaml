<Button x:Name="ButtonRoot"
        x:Class="Juniper.WinUI.App.Controls.AddableButtonUserControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:Juniper.WinUI.App.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Style="{StaticResource DateTimePickerFlyoutButtonStyle}"
        Command="{x:Bind OnCommand,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
        CommandParameter="{x:Bind ActionButtonCommandParam,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
        Visibility="{x:Bind ButtonVisibility,Mode=OneWay}"
        VerticalAlignment="Stretch"
        HorizontalAlignment="Left"
        Margin="8">
    <Button.Content>
        <Viewbox MaxWidth="18"
                 MaxHeight="18">
            <SymbolIcon Symbol="Add" />
        </Viewbox>
    </Button.Content>
</Button>
