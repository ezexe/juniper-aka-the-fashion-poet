<UserControl x:Class="Juniper.WinUI.App.Controls.RemoveableItemUserControl"
             xmlns:behaviors="using:Juniper.WinUI.App.Behaviors"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Juniper.WinUI.App.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="uc"
             BorderThickness="{StaticResource ControlBorderThickness}"
             Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
             BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
             CornerRadius="{StaticResource ControlCornerRadius}"
             Loaded="uc_Loaded"
             PointerEntered="ListViewSwipeContainer_PointerEntered"
             PointerExited="ListViewSwipeContainer_PointerExited">

    <Grid x:Name="controlRoot"
          VerticalAlignment="Stretch"
          HorizontalAlignment="Stretch"
          Padding="0"
          BorderThickness="{Binding ElementName=uc, Path=BorderThickness}"
          Background="{Binding ElementName=uc, Path=Background}"
          BorderBrush="{Binding ElementName=uc, Path=BorderBrush}"
          CornerRadius="{Binding ElementName=uc, Path=CornerRadius}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="56" />
        </Grid.ColumnDefinitions>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="HoveringStates">
                <VisualState x:Name="HoverButtonsHidden" />
                <VisualState x:Name="HoverButtonsShown">
                    <VisualState.Setters>
                        <Setter Target="HoverButton.Visibility"
                                Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <ContentControl x:Name="HeaderContentDisplay"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        VerticalContentAlignment="Stretch"
                        Margin="{StaticResource ActionsPadding}"
                        Content="{x:Bind ItemContent}"
                        IsEnabled="{Binding ForceEnabled,UpdateSourceTrigger=PropertyChanged}" />

        <!--<AppBarButton LabelPosition="Collapsed"
                      behaviors:AncestorSource.AncestorType="Page"
                      behaviors:AncestorSource.StatusType="False"
                      VerticalAlignment="Top"
                      HorizontalAlignment="Right"
                      Grid.Column="1"
                      Icon="Delete"
                      Width="32"
                      ToolTipService.ToolTip="Delete Item"
                      Click="AppBarButton_Click"/>-->
        <!--<SwipeControl x:Name="ListViewSwipeContainer"
                      Visibility="{Binding ForceEnabled,UpdateSourceTrigger=PropertyChanged,Converter={StaticResource boolToVisibilityConverter}}"
                      Grid.Column="1">
            <SwipeControl.RightItems>
                <SwipeItems Mode="Execute">
                    <SwipeItem x:Name="DeleteSwipeItem"
                               Background="Red"
                               Command="{x:Bind Command}" />
                </SwipeItems>
            </SwipeControl.RightItems>
            <Grid VerticalAlignment="Top"
                  HorizontalAlignment="Left">
                --><!--<TextBlock Text="Delete"
                           Margin="10"
                           FontSize="18"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center" />--><!--
                <Button x:Name="HoverButton"
                        Grid.Column="1"
                        Command="{x:Bind Command}"
                        ToolTipService.ToolTip="Delete Item"
                        Style="{StaticResource DateTimePickerFlyoutButtonStyle}"
                        Margin="{StaticResource ActionsPadding}"
                        VerticalAlignment="Top"
                        HorizontalAlignment="Left"
                        Visibility="Collapsed"
                        Click="AppBarButton_Click">
                    <Button.Content>
                        <SymbolIcon Symbol="Clear" />
                    </Button.Content>
                </Button>
            </Grid>
        </SwipeControl>-->

        <Button x:Name="HoverButton"
                Grid.Column="1"
                ToolTipService.ToolTip="Delete Item"
                VerticalAlignment="Top"
                HorizontalAlignment="Left"
                Visibility="Collapsed"
                Style="{StaticResource DateTimePickerFlyoutButtonStyle}"
                Margin="{StaticResource ActionsPadding}"
                Click="AppBarButton_Click"
                Command="{x:Bind Command}">
            <Button.Content>
                <Viewbox Width="{StaticResource SquareButtonIconSmall}"
                         Height="{StaticResource SquareButtonIconSmall}">
                    <SymbolIcon Symbol="Clear" />
                </Viewbox>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
