<Page
    x:Class="Juniper.WinUI.App.Pages.SendMultiInvoicePreviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Juniper.WinUI.App.Pages"
      xmlns:models="using:Juniper.Core.Models"
      xmlns:userControls="using:Juniper.WinUI.App.Controls"
      xmlns:viewModels="using:Juniper.Core.ViewModels"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <CollectionViewSource x:Name="CollectionOfCVS"
                              Source="{x:Bind ViewModel.PreviewASNInvoicesData}"
                              ItemsPath="InvoiceViewModels"
                              IsSourceGrouped="true" />
    </Page.Resources>
    <Grid>
        <ListView ItemsSource="{Binding Source={StaticResource CollectionOfCVS}}">
            <!--<ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel AreStickyGroupHeadersEnabled="True" />
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>-->
            <ListView.GroupStyle>
                <GroupStyle HeaderContainerStyle="{StaticResource GroupHeaderContainerStyle}"
                            HeaderTemplate="{StaticResource GroupHeaderTemplate}"
                            HidesIfEmpty="True" />
            </ListView.GroupStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModels:InvoiceViewModel"
                              x:Name="vmTemplate">
                    <userControls:RemoveableItemUserControl BorderThickness="0"
                                                            IsEnabled="{x:Bind ForceEnabled,UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"
                                                            RemoveClicked="RemoveableItemUserControl_RemoveClicked">
                        <userControls:RemoveableItemUserControl.ItemContent>
                            <Grid BorderThickness="{StaticResource ControlBorderThickness}"
                                  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                  CornerRadius="{StaticResource ControlCornerRadius}">
                                <Grid x:Name="controlRoot"
                                      Grid.Column="1"
                                      MinWidth="420">
                                    <StackPanel VerticalAlignment="Center"
                                                HorizontalAlignment="Stretch">

                                        <TextBlock HorizontalAlignment="Center"
                                                   Style="{StaticResource BodyTextBlockStyle}">
                                             <Run Text="{x:Bind InvoiceHeader.InvoiceDate, 
                                                            Converter={StaticResource StringFormatConverter},   
                                                            ConverterParameter=' {0:ddd MM-dd-yyyy}'}" />
                                        </TextBlock>

                                        <TextBlock HorizontalAlignment="Center"
                                                   Style="{StaticResource SubtitleTextBlockStyle}">
                                    <Run Text="{x:Bind PoId}" />
                                        </TextBlock>

                                        <TextBlock HorizontalAlignment="Center"
                                                   Style="{StaticResource SubtitleTextBlockStyle}">
                                  <Run Text="{x:Bind ASNId}" />
                                        </TextBlock>

                                        <TextBlock HorizontalAlignment="Center"
                                                   Style="{StaticResource SubtitleTextBlockStyle}">
                                    <Run Text="{x:Bind InvoiceHeader.InvoiceNumber}" />
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </userControls:RemoveableItemUserControl.ItemContent>
                    </userControls:RemoveableItemUserControl>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
