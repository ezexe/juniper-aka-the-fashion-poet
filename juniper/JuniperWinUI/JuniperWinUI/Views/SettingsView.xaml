<Page x:Class="JuniperWinUI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:JuniperWinUI.Views"
             xmlns:viewModels="using:Juniper.Core.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Page.DataContext>
        <viewModels:SettingsViewModel x:Name="ViewModel" />
    </Page.DataContext>
    <RelativePanel>
        <!--<CommandBar x:Name="MainCommandBar"
                    Margin="0,0,0,0"
                    Background="Transparent"
                    DefaultLabelPosition="Right"
                    RelativePanel.AlignRightWithPanel="True"
                    RelativePanel.RightOf="PageTitle">
            <AppBarButton x:Name="SaveOrderButton"
                          Command="{x:Bind ViewModel.SaveCommand}"
                          Icon="Save"
                          IsEnabled="{x:Bind ViewModel.IsModified, Mode=OneWay}"
                          Label="Save" />
        </CommandBar>-->
        
        <!--<local:APIConnectionInfoView Grid.Row="1" />-->
    </RelativePanel>
</Page>
